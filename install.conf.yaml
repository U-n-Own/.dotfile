- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.dotfiles: ''
    ~/.emacs.d:
    ~/.gitconfig:
    ~/.zprofile:

- create:
    - ~/downloads

- shell:
  - [git submodule update --init --recursive, Installing submodules]
