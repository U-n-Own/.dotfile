- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
   ~/.zshrc: zshrc
   ~/.dotfiles: ''
   ~/.emacs.d:
   ~/.zprofile:
   ~/.p10k.zsh:
   ~/.zsh_history:

- create:
    - ~/downloads

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
